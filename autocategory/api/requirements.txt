fastapi==0.115.5
uvicorn[standard]==0.32.1
gunicorn==21.2.0  # Production WSGI server
httpx==0.27.2
qdrant-client==1.12.1
pydantic==2.9.2
pydantic-settings==2.6.1
python-multipart==0.0.12

# Embeddings - Local Model (GPU - CUDA 12.1 for RTX 3060)
# Install with: pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cu121
sentence-transformers==3.3.1
torch==2.5.1+cu121

# Database & ORM
sqlalchemy==2.0.35
psycopg2-binary==2.9.10
alembic==1.14.0

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.1
email-validator==2.1.0

# Redis & Caching
redis==5.2.0
hiredis==3.0.0

# Gemini Web is handled by the gemini-proxy sidecar container (see autocategory/gemini-proxy/)
# — NOT imported here to avoid dependency conflicts with torch/sentence-transformers.

# Utilities
python-dotenv==1.0.1
psutil==6.1.0  # System monitoring

# Monitoring & Metrics
prometheus-client==0.20.0  # Prometheus metrics

# Testing (dev only)
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
